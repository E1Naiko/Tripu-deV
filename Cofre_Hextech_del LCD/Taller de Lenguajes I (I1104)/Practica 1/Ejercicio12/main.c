#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int num,cua,pot;
    double raiz;
    for (num=1; num <= 10; num++ ) {
        raiz = sqrt(num);
        cua = pow(num);
        pot =
    }
    return 0;
}
