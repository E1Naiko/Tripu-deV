#include <stdio.h>
#include <stdlib.h>
#include "istack.h"

int main() {
    Stack *p;
    int num;
    printf("Escribe un numero: \n");
    scanf("%d",&num);
    while ( num != 0 ) {
        p.pila = s_create();
        p.pila =
    }
    printf("Hello world!\n");
    return 0;
}
